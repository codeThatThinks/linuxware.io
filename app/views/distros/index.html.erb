<div class="page-header">
	<div class="pull-left"><h1>Linux Distros</h1></div>
	<div class="pull-right"><%= link_to "Add", new_distro_path, class: "btn btn-primary" %></div>
</div>
<br />
<div class="row">
	<div class="col-xs-12">
		<% if @distros.length == 0 %>
		<span>No distros found.</span>
		<% end %>
		<% @distros.each do |distro| %>
		<h2><%= link_to distro.name, distro %></h2>
		<% end %>
	</div>
</div>
