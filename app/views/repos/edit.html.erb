<div class="row">
	<div class="col-xs-12">
		<div class="page-header">
			<h1><%= link_to @distro.name, @distro %> &gt; Edit <%= @repo.name %></h1>
		</div>
		<%= render partial: 'form', locals: {url: @repo, method: :patch} %>
	</div>
</div>